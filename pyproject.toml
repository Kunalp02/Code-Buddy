[project]
name = "arcfold"
version = "0.1.0"
description = "Arcfold — local-first codebase intelligence with architecture maps and documentation"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["server*"]
